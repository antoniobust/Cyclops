<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.mdmobile.pocketconsole">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission
        android:name="android.permission.MANAGE_ACCOUNTS"
        android:maxSdkVersion="22"/>
    <uses-permission
        android:name="android.permission.USE_CREDENTIALS"
        android:maxSdkVersion="22"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.logIn.LoginActivity">
            <intent-filter>

                <!-- Searches intent -->
                <action android:name="android.intent.action.SEARCH"/>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/device_searchable"/>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/title_activity_main"
            android:launchMode="singleTop"/>

        <!-- Data provider -->
        <provider
            android:name=".provider.McProvider"
            android:authorities="com.mdmobile.pocketconsole"/>

        <!-- Services -->
        <service
            android:name=".services.RefreshDataService"
            android:enabled="true"
            android:exported="false"/>

        <!-- Account authenticator service -->
        <service android:name=".services.AuthenticationService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator"/>
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator"/>
        </service>
        <service
            android:name=".services.RefreshDataBinder"
            android:enabled="true"
            android:exported="true"/>

        <activity
            android:name=".ui.deviceDetails.DeviceDetailsActivity"
            android:label="@string/title_activity_device_details"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.mdmobile.pocketconsole.ui.main.MainActivity"/>
        </activity>
    </application>

</manifest>